<body class="<%= controller_name %> <%= action_name %>">

  <div class="container-form">

    <h1 class = "request-title">申請画面</h1>

    <%= form_with model: @attendance_request, url: attendance_requests_path, method: :post do |f| %>

      <div class="form-group">
        <div class="form-workDate">
          <%= f.label :target_date, "勤務日" %>
          <%= f.date_field :target_date, required: true %>
        </div>

        <div class="form-startTime">
          <%= f.label :requested_start_time, "出勤時刻" %>
          <%= f.time_field :requested_start_time, required: true %>
        </div>

        <div class="form-endTime">
          <%= f.label :requested_end_time, "退勤時刻" %>
          <%= f.time_field :requested_end_time, required: true %>
        </div>

        <div class="form-breakMinutes">
          <%= f.label :requested_break_minutes, "休憩時間（分）" %>
          <%= f.number_field :requested_break_minutes, min: 0, required: true %>
        </div>

        <div class="form-note">
          <%= f.label :reason, "申請理由/備考" %>
          <%= f.text_area :reason %>
        </div>

        <div class="form-actions">
          <%= f.submit "申請", class: "btn btn-primary" %>
        </div>
      </div>

    <% end %>

  </div>
</body>